import NavDrawerContainer from "./NavDrawer.styled";

const NavDrawer = (open) => (
  <>
    <NavDrawerContainer visible={open}>Hello How are you</NavDrawerContainer>
  </>
);

export default NavDrawer;
